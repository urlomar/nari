import s from "@/styles/Header.module.css";

/** Decorative header spacer (kept for future expansion). */
export default function Header() {
  return <div className={s.spacer} aria-hidden="true" />;
}
